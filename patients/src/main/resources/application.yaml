spring:
  application:
    name: patients

  cloud:
    config:
      enabled: false
    consul:
      host: consul
      port: 8500
      discovery:
        hostname: patients
        health-check-interval: 10s

  datasource:
    url: jdbc:mysql://mysql:3306/healthcare
    username: admin_healthcare
    password: healthadmin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health